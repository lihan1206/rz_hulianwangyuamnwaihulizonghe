export const pickUser = (user) => ({
  id: user.id,
  phone: user.phone,
  role: user.role,
  name: user.name,
  gender: user.gender,
  city: user.city,
  address: user.address,
  specialty: user.specialty,
  certNo: user.certNo,
  verified: user.verified,
  avatar: user.avatar
});
